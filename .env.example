# Phone-hosted LLM proxy (Cloudflare Pages Function)
# Set these in: Cloudflare dashboard → Pages project → Settings → Environment variables
# NEVER commit real values. The browser never sees these.

PHONE_LLM_BASE_URL=https://llm.jozobozo.org
PHONE_LLM_API_KEY=replace_with_my_real_key
PHONE_LLM_MODEL=llama-3.2-3b-instruct.q4km.gguf
